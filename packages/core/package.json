{
  "name": "@webgpu-cesium/core",
  "version": "0.0.0",
  "description": "数学、地理、时间、请求与调度、Worker 任务、错误类型",
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project core --config ../../vitest.config.ts"
  }
}
