{
  "name": "@webgpu-cesium/renderer",
  "version": "0.0.0",
  "description": "Render Graph、RenderItem、帧级 uniform；后续：材质、光照、阴影、后处理",
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project renderer"
  },
  "dependencies": {
    "@webgpu-cesium/core": "workspace:*",
    "@webgpu-cesium/rhi": "workspace:*",
    "@webgpu-cesium/shaders": "workspace:*"
  }
}
