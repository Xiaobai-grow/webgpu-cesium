# Changesets

全部 `@webgpu-cesium/*` 包采用 fixed 模式（同一版本号）。

- 新增变更：`pnpm changeset`，选择受影响的包与语义化级别，用中文描述变更。
- 生成版本与 changelog：`pnpm version-packages`。
- 0.x 阶段每个里程碑发一个 minor（见 `docs/10-architecture/02-packages.md`「版本与发布」）。

详见 [changesets 文档](https://github.com/changesets/changesets)。
